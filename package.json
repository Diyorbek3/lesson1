{
  "name": "express-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "Diyorbek",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
  "scripts": {
    "test": "jest",
    "test:unit": "jest tests/unit --verbose",
    "test:integration": "jest tests/integration --verbose",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --forceExit --bail",
    "test:watch": "jest --watch"
  }

}
